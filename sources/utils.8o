: tile_glitch_data
: sleep
	delay := va
	loop
		vf := delay
		if vf != 0 then
	again
	return

# i = banner address, va, vb, vc == text
: draw_banner
	clear
	draw_fullscreen_tile

	vd := va
	sleep_60
	va := vd
	draw_text

	#play short music sample?

: sleep_180
	va := 180
	jump sleep
